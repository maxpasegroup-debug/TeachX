# TeachX Guru Future Improvements

## Version 1.1 Recommendations

- Add a dedicated Open Graph launch image for social sharing.
- Convert large public hero images to AVIF/WebP variants after final photography is approved.
- Add production Lighthouse reporting to the release checklist.
- Add synthetic checks for `/`, `/login`, `/signup/teacher`, `/signup/student`, `/api/health`, `/api/ready`, and `/api/version`.
- Simplify legacy role labels in RBAC through a careful migration plan.
- Add stricter Content Security Policy after final provider domains are known.
- Add rate-limit provider configuration for public auth and AI entry points.
- Add Sentry or equivalent browser and server error monitoring.
- Add a launch analytics dashboard for public funnel tracking.
